{{- $data := include "fconet.envConfigMap" . | fromYaml }}
{{- if $data }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fconet.fullname" . }}-configmap
data:
  {{- toYaml $data | nindent 2 }}
{{- end }}
