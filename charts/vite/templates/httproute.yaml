{{- /* Rendered by the shared template in library/fc-common, so this behaviour
       lives in one place rather than in a copy per chart. The route's shape, its
       guards and the hostname splitting are all there. */}}
{{- include "fc.httproute" (dict "ctx" . "fullname" (include "vite.fullname" .) "labels" (include "vite.labels" .)) }}
